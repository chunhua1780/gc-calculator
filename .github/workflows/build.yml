name: Build APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Cordova
      run: npm install -g cordova

    - name: Build APK with keep-alive + local notification plugins
      run: |
        cordova create app com.ghostchat.calculator Calculator
        cp index.html app/www/index.html
        cp sw.js app/www/sw.js 2>/dev/null || true
        cp manifest.json app/www/manifest.json 2>/dev/null || true

        # icon文件复制到app根目录，供config.xml的<icon src="icon192.png">引用
        cp icon192.png app/icon192.png 2>/dev/null || true
        cp icon512.png app/icon512.png 2>/dev/null || true

        # 用我们准备好的config.xml覆盖默认生成的
        cp config.xml app/config.xml

        cd app
        cordova platform add android

        # ── 先装插件，再 prepare，最后 patch，最后 compile ─────────────────
        # 关键顺序说明：
        #   cordova build android = cordova prepare + Gradle
        #   prepare 会用 config.xml 重新生成 themes.xml，覆盖任何手动 patch。
        #   正确顺序：platform add → plugin add → prepare（生成最终文件）
        #             → 手动 patch（此时不会再被 prepare 覆盖）
        #             → cordova compile（只跑 Gradle，不再 prepare）
        # ──────────────────────────────────────────────────────────────────

        # 1. 安装所有插件（插件的 config-file 会在 prepare 时一并合并进去）
        cordova plugin add cordova-plugin-background-mode
        cordova plugin add cordova-plugin-local-notification@1.2.3
        cordova plugin add cordova-plugin-device
        cordova plugin add cordova-plugin-statusbar
        cordova plugin add cordova-plugin-camera
        cordova plugin add cordova-plugin-file

        # 2. 运行 prepare（生成最终平台文件，包含 themes.xml、AndroidManifest.xml 等）
        #    这是最后一次自动生成，之后的手动 patch 不会被覆盖
        cordova prepare android

        # ── 启动屏品牌化补丁（在 prepare 之后、compile 之前）────────────────
        PLATFORM_RES=platforms/android/app/src/main/res
        MANIFEST=platforms/android/app/src/main/AndroidManifest.xml

        # 3. 确保 colors.xml 存在并含有品牌色
        mkdir -p $PLATFORM_RES/values
        if [ ! -f $PLATFORM_RES/values/colors.xml ]; then
          printf '%s\n' '<?xml version="1.0" encoding="utf-8"?>' '<resources>' '</resources>' \
            > $PLATFORM_RES/values/colors.xml
        fi
        if ! grep -q 'cdv_splash_bg' $PLATFORM_RES/values/colors.xml; then
          sed -i 's|</resources>|    <color name="cdv_splash_bg">#E8E0FF</color>\n</resources>|' \
            $PLATFORM_RES/values/colors.xml
        fi

        # 4. 旧版 Android (<12)：在 themes.xml 里给 Theme.App.SplashScreen
        #    注入 android:windowBackground，让 Activity 窗口在 WebView 绘制前
        #    就显示品牌色，彻底消除白屏/默认图标
        if [ -f $PLATFORM_RES/values/themes.xml ] && \
           grep -q 'Theme.App.SplashScreen' $PLATFORM_RES/values/themes.xml && \
           ! grep -q 'cdv_splash_bg' $PLATFORM_RES/values/themes.xml; then
          sed -i '/<style name="Theme.App.SplashScreen"/a\        <item name="android:windowBackground">@color\/cdv_splash_bg<\/item>' \
            $PLATFORM_RES/values/themes.xml
          echo "[SplashBg] values/themes.xml patched"
        else
          echo "[SplashBg] values/themes.xml skip (already patched or not found)"
        fi
        echo "--- values/themes.xml after patch ---"
        cat $PLATFORM_RES/values/themes.xml 2>/dev/null || echo "(not found)"

        # 5. Android 12+ (API 31+)：patch 已有 values-v31/themes.xml，
        #    或在不存在时创建（不会 Duplicate，因为我们用不同文件名）
        mkdir -p $PLATFORM_RES/values-v31
        if [ -f $PLATFORM_RES/values-v31/themes.xml ] && \
           grep -q 'Theme.App.SplashScreen' $PLATFORM_RES/values-v31/themes.xml; then
          if ! grep -q 'cdv_splash_bg' $PLATFORM_RES/values-v31/themes.xml; then
            sed -i '/<style name="Theme.App.SplashScreen"/a\        <item name="windowSplashScreenBackground">@color\/cdv_splash_bg<\/item>' \
              $PLATFORM_RES/values-v31/themes.xml
            sed -i '/<style name="Theme.App.SplashScreen"/a\        <item name="windowSplashScreenIconBackgroundColor">@color\/cdv_splash_bg<\/item>' \
              $PLATFORM_RES/values-v31/themes.xml
          fi
          echo "[SplashBg] values-v31/themes.xml patched"
        else
          printf '%s\n' \
            '<?xml version="1.0" encoding="utf-8"?>' \
            '<resources>' \
            '    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">' \
            '        <item name="windowSplashScreenBackground">@color/cdv_splash_bg</item>' \
            '        <item name="windowSplashScreenIconBackgroundColor">@color/cdv_splash_bg</item>' \
            '        <item name="postSplashScreenTheme">@style/Theme.AppCompat.NoActionBar</item>' \
            '    </style>' \
            '</resources>' \
            > $PLATFORM_RES/values-v31/splash_theme.xml
          echo "[SplashBg] values-v31/splash_theme.xml created"
        fi

        # 6. 确保 MainActivity 使用品牌启动主题
        if grep -q 'android:theme=' $MANIFEST; then
          sed -i 's|android:theme="[^"]*"|android:theme="@style/Theme.App.SplashScreen"|g' $MANIFEST
        else
          sed -i 's|<activity android:configChanges|<activity android:theme="@style/Theme.App.SplashScreen" android:configChanges|g' $MANIFEST
        fi
        echo "[SplashTheme] AndroidManifest theme:"
        grep 'android:theme' $MANIFEST || echo "  (no theme attr found)"

        # 7. 键盘 adjustResize 双重保险
        if grep -q 'windowSoftInputMode' $MANIFEST; then
          sed -i 's/android:windowSoftInputMode="[^"]*"/android:windowSoftInputMode="adjustResize"/g' $MANIFEST
        else
          sed -i 's/<activity /<activity android:windowSoftInputMode="adjustResize" /g' $MANIFEST
        fi
        echo "[adjustResize] AndroidManifest:"
        grep 'windowSoftInputMode' $MANIFEST || echo "  (no match found)"
        # ──────────────────────────────────────────────────────────────────

        # 3. 只编译，不再 prepare（patch 内容完整保留到 APK）
        cordova compile android

    - name: Find APK
      run: find . -name "*.apk" 2>/dev/null

    - uses: actions/upload-artifact@v4
      with:
        name: GhostChat-Calculator-APK
        path: '**/*.apk'
        retention-days: 30
